services:
  tc002:
    image: ghcr.io/doitian/ulanzi-tc002:latest
    build: .
    ports:
      - "8008:8008"
    # TC002_DEVICE_IP / TC002_DEVICE_NETWORK if LAN scan cannot see the clock
    volumes:
      - tc002-data:/data

volumes:
  tc002-data:
